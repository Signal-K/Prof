import { MintNFTParams, MintNFTReturnType, NFT, NFTContract, RequiredParam, WalletAddress, useNFTBalanceParams } from "../../types";
import type { QueryAllParams, SmartContract, ValidContractInstance } from "@thirdweb-dev/sdk";
import { Erc721 } from "@thirdweb-dev/sdk";
import { BigNumber, BigNumberish } from "ethers";
/**
 @internal
 */
export declare function detectErc721Instance(contract: RequiredParam<ValidContractInstance | SmartContract | null>): Erc721<any> | undefined;
export declare function detectErc1155Instance(contract: RequiredParam<ValidContractInstance | SmartContract | null>): import("@thirdweb-dev/sdk").EditionDrop | import("@thirdweb-dev/sdk").Edition | undefined;
/** **********************/
/**     READ  HOOKS     **/
/** **********************/
/**
 * Use this to get an individual NFT token of your {@link NFTContract}.
 *
 * @example
 * ```javascript
 * const { data: nft, isLoading, error } = useNFT(NFTContract, <tokenId>);
 * ```
 *
 * @param contract - an instace of a {@link NFTContract}
 * @param tokenId - the tokenId to look up
 * @returns a response object that includes the metadata for the given tokenId
 * @beta
 */
export declare function useNFT<TContract extends NFTContract>(contract: RequiredParam<TContract>, tokenId: RequiredParam<BigNumberish>): import("react-query").UseQueryResult<NFT<TContract>, unknown>;
/**
 * Use this to get a list of NFT tokens of your {@link NFTContract}.
 *
 * @example
 * ```javascript
 * const { data: nfts, isLoading, error } = useNFTs(NFTContract, { start: 0, count: 100 });
 * ```
 *
 * @param contract - an instace of a {@link NFTContract}
 * @param queryParams - query params to pass to the query for the sake of pagination
 * @returns a response object that includes an array of NFTs
 * @beta
 */
export declare function useNFTs<TContract extends NFTContract>(contract: RequiredParam<TContract>, queryParams?: QueryAllParams): import("react-query").UseQueryResult<NFT<TContract>[], unknown>;
/**
 * Use this to get a the total (minted) supply of your {@link NFTContract}.
 *
 * @example
 * ```javascript
 * const { data: totalSupply, isLoading, error } = useNFTSupply(NFTContract);
 * ```
 *
 * @param contract - an instace of a {@link NFTContract}
 * @returns a response object that incudes the total minted supply
 * @beta
 */
export declare function useTotalCirculatingSupply(contract: RequiredParam<NFTContract>): import("react-query").UseQueryResult<BigNumber, unknown>;
/**
 * Use this to get a the owned NFTs for a specific {@link NFTContract} and wallet address.
 *
 * @example
 * ```javascript
 * const { data: ownedNFTs, isLoading, error } = useOwnedNFTs(NFTContract, <OwnerWalletAddress>);
 * ```
 *
 * @param contract - an instace of a {@link NFTContract}
 * @param ownerWalletAddress - the wallet adress to get owned tokens for
 * @returns a response object that includes the list of owned tokens
 * @beta
 */
export declare function useOwnedNFTs<TContract extends NFTContract>(contract: RequiredParam<TContract>, ownerWalletAddress: RequiredParam<WalletAddress>): import("react-query").UseQueryResult<NFT<TContract>[], unknown>;
/**
 * Use this to get a the total balance of a {@link NFTContract} and wallet address.
 *
 *
 * @example
 * ```javascript
 * const { data: ownerBalance, isLoading, error } = useNFTBalance(NFTContract, <OwnerWalletAddress>);
 * ```
 *
 * @param contract - an instace of a {@link NFTContract}
 * @param ownerWalletAddress - the wallet adress to check the balance of
 * @returns a response object that includes the total balance of the owner
 * @beta
 */
export declare function useNFTBalance<TContract extends NFTContract>(...[contract, ownerWalletAddress, tokenId]: useNFTBalanceParams<TContract>): import("react-query").UseQueryResult<BigNumber, unknown>;
/** **********************/
/**     WRITE HOOKS     **/
/** **********************/
/**
 * Use this to mint a new NFT on your {@link NFTContract}
 *
 * @example
 * ```jsx
 * const Component = () => {
 *   const {
 *     mutate: mintNft,
 *     isLoading,
 *     error,
 *   } = useMintNFT(NFTContract);
 *
 *   if (error) {
 *     console.error("failed to mint nft", error);
 *   }
 *
 *   return (
 *     <button
 *       disabled={isLoading}
 *       onClick={() => mintNft({ name: "My awesome NFT!" })}
 *     >
 *       Mint!
 *     </button>
 *   );
 * };
 * ```
 *
 * @param contract - an instace of a {@link NFTContract}
 * @returns a mutation object that can be used to mint a new NFT token to the connected wallet
 * @beta
 */
export declare function useMintNFT<TContract extends NFTContract>(contract: RequiredParam<TContract>): import("react-query").UseMutationResult<MintNFTReturnType<TContract>, unknown, MintNFTParams<TContract>, unknown>;
