import Header from "./header";
import Dashboard from "../pages/Dashboard";

const dbLayout = ({}) => (
  <>
    <Header />
    <main>
      <Dashboard />
    </main>
  </>
);

export default dbLayout;
